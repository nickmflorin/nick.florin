# Backlog

The running list of polish items, organized by page/area. Check items off as they land (`[x]`), and
add new items to the appropriate section as they come up (create a new section if none fits). Items
are independent — each lands on its own branch/PR.

## GreenBudget Project Page

- [ ] Shrink the displayed sizes of the images that are lower resolution, so they are not scaled up
      past what their source resolution supports — including the mobile images and the PDF-export
      images on the page.

## Site-Wide Transitions

- [ ] Use fade-in/fade-out transitions when navigating between content views that currently swap
      abruptly: navigating between the project tabs (the `/projects/*` pages), switching between
      different drawer content views while a drawer is open, and similar view-to-view swaps
      elsewhere in the app. The goal is that replacing one view with another reads as a transition
      rather than a hard cut; inventory the qualifying surfaces as part of the item.
