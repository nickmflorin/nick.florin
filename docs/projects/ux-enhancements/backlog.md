# Backlog

The running list of polish items, organized by page/area. Check items off as they land (`[x]`), and
add new items to the appropriate section as they come up (create a new section if none fits). Items
are independent — each lands on its own branch/PR.

## GreenBudget Project Page

- [ ] Shrink the displayed sizes of the images that are lower resolution, so they are not scaled up
      past what their source resolution supports — including the mobile images and the PDF-export
      images on the page.
