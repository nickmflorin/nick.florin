/* If this default is not used, the drawer will stay open when revisiting the page from another
   admin page even if the query parameter(s) are not present. */
export default function Default() {
  return null;
}
