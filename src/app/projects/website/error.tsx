"use client";
import { ErrorView } from "~/components/views/error/ErrorView";

export default function Error() {
  return <ErrorView>There was an error loading the project. Do not worry, we are on it.</ErrorView>;
}
